normalization1 = normalization([0.0, 0.0, 0.0], [255.0, 255.0, 255.0])
change_output_activation(conv80, sigmoid)
change_output_activation(conv99, sigmoid)
change_output_activation(conv117, sigmoid)
model_optimization_config(calibration, batch_size=2)
post_quantization_optimization(finetune, policy=enabled, learning_rate=0.00001)

shortcut1 = shortcut(matmul1, [reduce_max_softmax1, ew_sub_softmax1])

allocator_param(automatic_reshapes=disabled)
